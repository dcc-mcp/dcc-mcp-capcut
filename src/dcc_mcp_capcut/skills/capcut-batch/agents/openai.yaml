# Interface descriptor for CapCut Batch
#
# Consumed by agent UIs that read the `interface` block: a human display name,
# a one-line summary, and the default prompt to start a session from.
#
# This file is deliberately not a second source of truth. The skill name and the
# capability statement belong to the `SKILL.md` frontmatter, and
# `tests/test_interface_descriptors.py` fails when this file stops agreeing with
# it. Keep `short_description` at or under 64 characters and draw its wording
# from the frontmatter `description`.

interface:
  display_name: "CapCut Batch"
  short_description: "Turn one template plus N variable sets into N deliverables"
  # A batch needs two inputs before it can render anything: the template and
  # the variable sets. The manifest is a third argument and it means something
  # different -- it is where the batch records its own state, and it is what a
  # later `resume=true` reads back. Naming only the manifest is the mistake
  # this prompt exists to prevent, so the two inputs come first.
  default_prompt: "Render the template I give you with the variable sets I give you, and pass a manifest path so an interrupted batch resumes; report the receipt for each deliverable as it finishes."
